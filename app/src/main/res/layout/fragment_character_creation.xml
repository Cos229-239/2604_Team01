<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Typing in the Name -->
        <EditText
            android:id="@+id/editName"
            android:hint="Name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Pick a Race -->
        <Spinner
            android:id="@+id/spinnerRace"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:prompt="@string/select_race" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="16dp"/>

        <!-- Pick a Class -->
        <Spinner
            android:id="@+id/spinnerClass"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:prompt="@string/select_class"/>

        <Space
            android:layout_width="match_parent"
            android:layout_height="16dp"/>


        <!-- Ability Scores Start -->
        <TextView
            android:text="Ability Scores"
            android:textSize="18sp"
            android:layout_marginTop="16dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <!-- RecycleView for the creation of the 6 ability scores -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerAbilityScores"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <Space
            android:layout_width="match_parent"
            android:layout_height="16dp"/>

        <!-- Create the Character -->
        <Button
            android:id="@+id/btnCreate"
            android:text="Create Character"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"/>

    </LinearLayout>

</ScrollView>
